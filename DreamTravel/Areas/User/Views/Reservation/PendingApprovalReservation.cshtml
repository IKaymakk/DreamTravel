
@{
    ViewData["Title"] = "PendingApprovalReservation";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<div class="container-fluid py-4">
    <h2>Onay Bekleyen Rezervasyonlarım</h2>
    <div class="row ">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header pb-0">
                </div>
                <div class="card-body px-0 pt-0 pb-2">
                    <div class=" p-0">
                        <table style="width:100%" class="text-center table align-items-center mb-0">
                            <thead>
                                <tr>
                                    <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Sosyal Medya</th>
                                    <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 ps-2">Adı</th>
                                    <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Hakkında</th>
                                    <th class="text-secondary opacity-7"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @* @foreach (var x in Model)
                                {
                                <tr>
                                <td>
                                <div class="d-flex px-2 py-1">
                                <div>
                                <img style="width:100px;height:70px;border-radius:20px" src="@x.Image" class=" me-3" alt="user6">
                                </div>
                                <div class="d-flex flex-column justify-content-center">
                                <h6 class="mb-0 text-sm">Miriam Eric</h6>
                                <p class="text-xs text-secondary mb-0">@x.InstagramUrl</p>
                                <p class="text-xs text-secondary mb-0">@x.TwitterUrl</p>
                                </div>
                                </div>
                                </td>
                                <td>
                                <p class="text-xs font-weight-bold mb-0">@x.Name</p>
                                </td>
                                <td>
                                <p class="text-xs font-weight-bold mb-0">@x.Description</p>
                                </td>
                                </tr>
                                }*@
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>
